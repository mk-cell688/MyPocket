# 📘 [My Pocket] 제품 요구사항 문서 (PRD) v1.5

## 1. 개요 (Overview)
* **제품명:** **My Pocket (마이 포켓)**
* **슬로건:** 내 주머니 속 든든한 영어 버디
* **제품 목적:** 한국어 사용자가 여행·일상·비즈니스 상황에서 필요한 영어 표현을 플래시카드로 학습하고, AI를 통해 자신만의 맞춤 카드를 생성/추가/관리하는 웹 기반 학습 서비스.
* **핵심 가치:** * **Quick:** 막히는 문장을 AI로 즉시 해결.
    * **Personal:** 엑셀 업로드와 AI 영작으로 나만의 덱 구축.
    * **Light:** 설치 없이 웹에서 가볍게, 데이터는 내 브라우저에 안전하게. 엑셀로 다운도 가능하고 import로 추가도 가능

---

## 2. 기능 요구사항 (Functional Requirements)

### 2.1 스마트 플래시카드 학습
* **카테고리 시스템:** [전체], [여행], [일상], [비즈니스], [내 표현] 5개 탭 제공.
* **학습 인터랙션:** * 앞면(영어) -> 클릭 시 3D Flip 애니메이션 -> 뒷면(한국어+팁+예문) 노출.
    * **[✓ 외웠어요]**: 해당 카드 완료 처리 및 다음 카드 이동.
    * **[↺ 다시]**: 현재 남은 카드 더미의 뒤쪽으로 재배치 (최소 3~5장 간격 유지).
* **학습 완료:** 모든 카드 소진 시 완료 화면 노출 및 "학습 기록 초기화" 버튼 제공.
* **게이미피케이션:** 5회 연속 "외웠어요" 클릭 시 상단 토스트 메시지("Good job! 🔥") 노출.

### 2.2 AI 영작 및 카드 추가
* **자연어 처리:** Google Gemini API를 연동하여 자연스러운 표현 생성.
* **상황별 최적화:** 유저가 선택한 카테고리(여행/일상/비즈)와 표현 톤 선택 범위(비지니스/일상/캐주얼)에 맞춰 톤앤매너 조절.
* **즉시 카드화:** 영작 결과 하단의 **[+ 포켓에 넣기]** 버튼 클릭 시 해당 카테고리와 [내 표현] 탭에 동시 저장.
* **중복 방지:** 이미 추가된 문장은 버튼을 비활성화하여 중복 생성 방지.

### 2.3 데이터 관리 (Excel & Storage)
* **로컬 저장소:** `localStorage`를 활용해 로그인 없이도 기기 내 데이터 유지.
* **엑셀 내보내기(Export):** 선택한 카테고리의 데이터를 CSV 파일로 백업.
* **엑셀 가져오기(Import):** 규격화된 CSV 파일을 업로드하여 대량의 단어장을 한 번에 등록.
* **샘플 가이드:** 업로드 화면 내 샘플 CSV 다운로드 링크 및 작성 가이드 제공.

---

## 3. UI/UX 가이드라인

### 3.1 디자인 시스템
* **메인 컬러:** `#313E5D` - 스마트하고 신뢰감 있는 톤.
* **포인트 컬러:** 여행(#629CC5), 일상(#), 비즈니스(#E6F1FB).
* **레이아웃:** 모바일 우선(최대 너비 560px), 중앙 정렬 배치.

### 3.2 화면 구성
1.  **Home (Study):** 상단 카테고리 탭 -> 중앙 플래시카드 -> 하단 진행률 바 및 액션 버튼.
2.  **AI Buddy (Writer):** 한글 입력창(Placeholder 제공) -> 카테고리 선택 -> [영작하기] 버튼 -> 결과 카드.
3.  **My List:** 내 카드 목록(리스트 형태) -> 카드 개별 삭제(X) -> 하단 [엑셀 업로드/다운로드] 버튼.

---

## 4. 비기능 요구사항
* **속도:** AI 영작 응답 평균 2초 이내 유지.
* **안내문:** "데이터가 브라우저에 저장됩니다"라는 안내 문구를 [My List] 상단에 명시.
* **반응형:** 데스크탑에서도 모바일 뷰(Phone UI)를 유지하여 일관된 경험 제공.

---

## 5. 데이터 구조 (Data Schema)
```json
{
  "id": "item_17123456789", // 생성 타임스탬프 기반 고유 ID
  "cat": "travel | daily | biz",
  "en": "Where can I get a taxi?",
  "ko": "택시 어디서 타나요?",
  "usage": "길거리에서 현지인에게 묻기 좋은 표현입니다.",
  "source": "preset | user", // 기본 제공(preset)인지 유저 생성(user)인지 구분
  "done": false, // 학습 완료 여부
  "createdAt": "2024-05-20"
}
```

---

## 6. 향후 로드맵
* **v1.2:** Web Speech API를 활용한 원어민 발음 듣기 기능 추가.
* **v1.3:** 스페이스드 리피티션(망각 곡선) 기반 자동 복습 알림.
* **v2.0:** 클라우드 동기화(로그인) 및 친구와 카드 덱 공유 기능.

